# SkoolCampus Backend Configuration
# Copy this file to .env and update with your settings

# Database Configuration
# Update with your MySQL credentials
DATABASE_URL="mysql://username:password@localhost:3306/database_name"
# Example: DATABASE_URL="mysql://ramom:123456@localhost:3306/ramom"

# JWT Configuration
# IMPORTANT: Change this to a random, secure secret key for production!
# You can generate one using: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRATION="7d"

# Application Configuration
PORT=3000
NODE_ENV=development

# Email Configuration (for password reset)
# Update with your SMTP server details
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USER=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_FROM=noreply@skoolcampus.com

# Frontend URL (for password reset links and CORS)
FRONTEND_URL=http://localhost:4200

# File Upload Configuration
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# Optional: Uncomment and configure if needed
# REDIS_HOST=localhost
# REDIS_PORT=6379
# SESSION_SECRET=your-session-secret
